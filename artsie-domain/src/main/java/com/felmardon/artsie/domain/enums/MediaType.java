package com.felmardon.artsie.domain.enums;

/**
 * Represents the type of media stored in the system.
 * PHOTO is the primary focus; VIDEO support is planned for future releases.
 */
public enum MediaType {
    PHOTO,
    VIDEO
}
